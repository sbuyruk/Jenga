@model IEnumerable<MarkaTanim>
@{
    ViewData["Title"] = "Index";
}

<div class="container-sm shadow p-3">
    <div class="row">
        <div class="col ">
            <h4 class="text-primary text-start fw-bold">Marka Tanımları</h4>
        </div>
        <div class="col text-end">
            <a asp-controller="MarkaTanim" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Yeni Marka Tanımı
            </a>
        </div>
    </div>
    <hr />
    <div class="pt-3">
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Marka
                    </th>
                    <th>
                        Açıklama
                    </th>
                    <th>
                        Aktif
                    </th>
                    <th>
                        Düzenle/Sil
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="30%">
                            @obj.Adi
                        </td>
                        <td width="40%">
                            @obj.Aciklama
                        </td>
                        <td width="10%">
                            <div class="checkbox mb-3 col-2 ">
                                <input asp-for=@obj.Aktif class="form-check" type="checkbox" disabled />
                            </div>
                        </td>
                        <td width="30%">
                            <div class="btn-group" role="group">
                                <a asp-controller="MarkaTanim" asp-action="Edit" asp-route-id="@obj.Id"
                               class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Düzenle
                                </a>
                                <a asp-controller="MarkaTanim" asp-action="Delete" asp-route-id="@obj.Id"
                               class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Sil
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
