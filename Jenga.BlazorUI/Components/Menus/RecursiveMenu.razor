@using Jenga.Models.Common
@using Microsoft.AspNetCore.Components.Web
@inject NavigationManager Nav

<ul class="recursive-menu">
    @foreach (var item in Items ?? Enumerable.Empty<MenuItem>())
    {
        <li class="menu-item @(item.IsExpanded ? "open" : "") @(item.IsActive ? "active" : "")">
            @if (!string.IsNullOrEmpty(item.Url) && (item.Children == null || !item.Children.Any()))
            {
                <a href="@item.Url"
                   class="menu-header nav-link"
                   @onclick="(e) => OnNavigate(e, item.Url)"
                   @onclick:preventDefault>
                    <span class="menu-title">@item.Title</span>
                </a>
            }
            else
            {
                <div class="menu-header"
                     @onclick="() => Toggle(item)"
                     @onclick:stopPropagation>
                    <span class="menu-title">@item.Title</span>
                    @if (item.Children?.Any() == true)
                    {
                        <span class="toggle-icon @(item.IsExpanded ? "rotated" : "")">
                            <svg class="menu-icon" width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M8 5v14l11-7z" />
                            </svg>
                        </span>
                    }
                </div>
            }

            @if (item.IsExpanded && item.Children?.Any() == true)
            {
                <ul class="submenu show">
                    <RecursiveMenu Items="item.Children" />
                </ul>
            }
        </li>
    }
</ul>

@code {
    [Parameter]
    public List<MenuItem> Items { get; set; } = new();

    protected override void OnInitialized()
    {
        // update active flags at startup
        SetActiveRecursive(Items);
        Nav.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, Microsoft.AspNetCore.Components.Routing.LocationChangedEventArgs e)
    {
        SetActiveRecursive(Items);
        InvokeAsync(StateHasChanged);
    }

    private void Toggle(MenuItem item)
    {
        if (item == null) return;

        item.IsExpanded = !item.IsExpanded;

        // mark the clicked parent as selected (visual feedback)
        if (Items != null)
        {
            ClearActiveRecursive(Items);
            item.IsActive = true;
        }
    }

    private void SetActiveRecursive(List<MenuItem> items)
    {
        if (items == null) return;

        var fullUri = Nav.Uri.ToLowerInvariant();
        var currentUrl = Nav.ToBaseRelativePath(Nav.Uri).TrimStart('/').ToLowerInvariant();

        foreach (var item in items)
        {
            var normalizedItemUrl = item.Url?.TrimStart('/').ToLowerInvariant();
            item.IsActive = (!string.IsNullOrWhiteSpace(normalizedItemUrl)
                             && (currentUrl == normalizedItemUrl || fullUri.EndsWith(normalizedItemUrl)));

            if (item.Children?.Any() == true)
            {
                SetActiveRecursive(item.Children);
                if (item.Children.Any(c => c.IsActive))
                    item.IsActive = true;
            }

            // Ensure the branch that contains the active item stays expanded so the selected menu is visible after navigation
            item.IsExpanded = item.IsActive || (item.Children?.Any(c => c.IsActive) == true);
        }
    }

    private void ClearActiveRecursive(List<MenuItem> items)
    {
        if (items == null) return;
        foreach (var i in items)
        {
            i.IsActive = false;
            if (i.Children?.Any() == true) ClearActiveRecursive(i.Children);
        }
    }

    // Find menu item by URL and mark it (expand ancestors) so selection is immediate on click
    private bool MarkActiveByUrl(List<MenuItem> items, string normalizedUrl)
    {
        if (items == null) return false;
        foreach (var item in items)
        {
            var itemUrl = item.Url?.TrimStart('/').ToLowerInvariant() ?? "";
            if (!string.IsNullOrEmpty(itemUrl) && itemUrl == normalizedUrl)
            {
                ClearActiveRecursive(Items ?? new List<MenuItem>());
                item.IsActive = true;
                return true;
            }

            if (item.Children?.Any() == true)
            {
                if (MarkActiveByUrl(item.Children, normalizedUrl))
                {
                    item.IsExpanded = true;
                    item.IsActive = true;
                    return true;
                }
            }

            // not matched here
            item.IsActive = item.IsActive && item.IsActive; // keep any previous true set for ancestors handled above
        }

        return false;
    }

    private void OnNavigate(MouseEventArgs e, string url)
    {
        if (string.IsNullOrWhiteSpace(url))
            return;

        // Normalize incoming url for matching
        var normalized = url.StartsWith("/") ? url.TrimStart('/').ToLowerInvariant() : url.TrimStart('/').ToLowerInvariant();

        // mark menu immediately so user sees selection before navigation completes
        if (Items != null)
            _ = MarkActiveByUrl(Items, normalized);

        // Normalize URLs and navigate
        if (Uri.TryCreate(url, UriKind.Absolute, out var abs))
        {
            try
            {
                var baseUri = new Uri(Nav.BaseUri);
                if (string.Equals(abs.Host, baseUri.Host, StringComparison.OrdinalIgnoreCase)
                    && string.Equals(abs.Scheme, baseUri.Scheme, StringComparison.OrdinalIgnoreCase))
                {
                    var path = abs.PathAndQuery + abs.Fragment;
                    Nav.NavigateTo(path, forceLoad: false);
                    return;
                }
            }
            catch
            {
                // fallback below
            }

            // external/other host -> full load
            Nav.NavigateTo(url, forceLoad: true);
            return;
        }

        // relative/partial path
        var to = url.StartsWith("/") ? url : "/" + url;
        Nav.NavigateTo(to, forceLoad: false);
    }

    public void Dispose()
    {
        Nav.LocationChanged -= OnLocationChanged;
    }
}