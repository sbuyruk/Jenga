@page "/test/toast"
@rendermode InteractiveServer
@inject IToastService ToastService

<h3>Toast Test Sayfasi</h3>

<div class="d-flex flex-column gap-2 mt-3">
    <button class="btn btn-info" @onclick="@(() => ShowToast("Bilgilendirme mesaji", ToastType.Info))">Info</button>
    <button class="btn btn-success" @onclick="@(() => ShowToast("Islem basarili!", ToastType.Success))">Success</button>
    <button class="btn btn-warning" @onclick="@(() => ShowToast("Dikkat edilmesi gereken durum", ToastType.Warning))">Warning</button>
    <button class="btn btn-danger" @onclick="@(() => ShowToast("Hata olustu!", ToastType.Error))">Error</button>


</div>

@code {
    private void ShowToast(string message, ToastType type)
    {
        ToastService.ShowToast(message, type, 3000);
    }

}
